{
  "name": "shelf-lookup",
  "version": "1.0.0",
  "private": true,
  "description": "Database-backed barcode, shelf, and artist-sorting workflow for KTRU's physical music collection.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check resolve-artists.js && node --check artist-resolution.js",
    "test": "node --test test/*.test.js",
    "resolve:artists": "node resolve-artists.js",
    "review:artists": "node resolve-artists.js --review"
  },
  "keywords": [
    "ktru",
    "music-library",
    "catalog",
    "sqlite",
    "express"
  ],
  "author": "Matthew Bitz",
  "license": "ISC",
  "dependencies": {
    "better-sqlite3": "^12.6.2",
    "cors": "^2.8.6",
    "express": "^5.2.1"
  }
}
